document.addEventListener('DOMContentLoaded',()=>{const search=document.querySelector('.patients-toolbar input[name="q"]');if(search&&!search.value)search.focus({preventScroll:true});document.querySelectorAll('[data-delete-patient]').forEach(button=>button.addEventListener('click',()=>{const name=button.dataset.patientName||'este paciente';if(window.confirm(`Excluir o paciente "${name}"? Esta ação não pode ser desfeita.`)){document.getElementById(`delete-form-${button.dataset.deletePatient}`)?.submit();}}));});
